import React from 'react';
import PropTypes from 'prop-types';
import { compose } from 'recompose';
import styles from './styles';

class DebtsPage extends React.PureComponent {
  render() {
    return (
      <div />
    );
  }
}

DebtsPage.propTypes = {
};

export default compose()(DebtsPage);
